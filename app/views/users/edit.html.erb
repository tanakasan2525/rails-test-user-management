<% @title = 'Edit Account' %>
<h1><%= @title %></h1>

<div id='generic-form'>
	<%= form_for @user, url: :account do |f| %>
		<%= render 'form', f: f %>
		<div class='buttons'>
			<%= f.submit 'Update' %>
			<%= link_to 'Cancel', :account %>
		</div>
	<% end %>
</div>

