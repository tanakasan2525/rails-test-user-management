<% @title = 'Sign up' %>
<h1><%= @title %></h1>

<div id='generic-form'>
	<%= form_for [ :admin, @user ] do |f| %>
		<%= render 'form', f: f %>
		<div class='buttons'>
			<%= f.submit 'Register' %>
			<%= link_to 'Cancel', :admin_users %>
		</div>
	<% end %>
</div>
